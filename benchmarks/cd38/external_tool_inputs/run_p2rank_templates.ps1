# Generated by prepare_cd38_external_tool_inputs.py
# Run external P2Rank commands for CD38 target structures.
$ErrorActionPreference = 'Stop'
$PackageRoot = Split-Path -Parent $MyInvocation.MyCommand.Path

# 3ROP
$PdbPath = Join-Path $PackageRoot "pdbs\3ROP.pdb"
$OutDir = Join-Path $PackageRoot "p2rank_outputs\3ROP"
New-Item -ItemType Directory -Force -Path $OutDir | Out-Null
prank predict -f $PdbPath -o $OutDir

# 4OGW
$PdbPath = Join-Path $PackageRoot "pdbs\4OGW.pdb"
$OutDir = Join-Path $PackageRoot "p2rank_outputs\4OGW"
New-Item -ItemType Directory -Force -Path $OutDir | Out-Null
prank predict -f $PdbPath -o $OutDir

# 3F6Y
$PdbPath = Join-Path $PackageRoot "pdbs\3F6Y.pdb"
$OutDir = Join-Path $PackageRoot "p2rank_outputs\3F6Y"
New-Item -ItemType Directory -Force -Path $OutDir | Out-Null
prank predict -f $PdbPath -o $OutDir

